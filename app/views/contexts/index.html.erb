<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Contexts
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Title
            </th>

            <th>
              Credential
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @contexts, partial: "table_row", as: :context %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @contexts.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :context %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Contexts
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @contexts, partial: "list_item", as: :context %>
      </div>
    </div>
  </div>
</div>
